                // Switch hash key based on chosen comparison mode.
                switch (mode){
                    case Name:
                        candidates.put(path.getFileName().toString(), path.toString());
                        break;
                    case Size:
                        try {
                            candidates.put(String.format("%d", Files.size(path)), path.toString());
                        } catch (IOException e) {
                            // throw new RuntimeException(e);
                            // TODO
                        }
                    default:
                        try {
                            candidates.put(String.format("%d%s", Files.size(path), path.getFileName()), path.toString());
                        } catch (IOException e) {
                            // throw new RuntimeException(e);
                            // TODO
                        }
                }
